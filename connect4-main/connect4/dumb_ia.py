from .game import Player


class DumbIA(Player):
    def play(self, grid):
      #   cell = 0
      #  colons = 0

      # for cell in grid[line]:

        #    for colons in grid[column]:
        #        if cell != color:
        #           if colons != color:
       # Player.play(self,IA,5)
